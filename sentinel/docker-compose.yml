version: '3.8'
services:
  redis:
    image: redis:alpine
    container_name: sentinel_redis
    ports:
      - "6379:6379"
    # Persist data so we don't lose it on restart
    volumes:
      - ./redis_data:/data